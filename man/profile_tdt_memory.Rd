% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{profile_tdt_memory}
\alias{profile_tdt_memory}
\title{Profile memory movement for a TDT read workflow}
\usage{
profile_tdt_memory(
  block_path,
  ...,
  stream = NULL,
  events = TRUE,
  summarize = TRUE,
  quiet = TRUE
)
}
\arguments{
\item{block_path}{Path to a TDT block directory.}

\item{...}{Arguments passed to \code{\link[=read_block]{read_block()}}.}

\item{stream}{Optional stream store to collect after the read. Leave \code{NULL}
to profile metadata and events without copying stream data into R.}

\item{events}{If \code{TRUE}, collect epocs/events after the read.}

\item{summarize}{If \code{TRUE}, call \code{\link[=summary]{summary()}} on the Python-backed block.}

\item{quiet}{Suppress collection size warnings.}
}
\value{
A tibble with one row per profiled step.
}
\description{
\code{profile_tdt_memory()} runs a small read workflow and records elapsed time,
R allocations reported by \code{\link[=Rprofmem]{Rprofmem()}}, and Python allocations reported by
Python \code{tracemalloc} when available. Use reader arguments such as \code{store},
\code{channel}, \code{t1}, \code{t2}, \code{evtype}, and \code{ranges} to test a bounded workflow
before scaling up to a larger block.
}
\details{
The numbers are diagnostics, not portable benchmarks. They are most useful
for comparing alternative reads on the same machine and Python environment.
}
