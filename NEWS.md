# tdtr 0.0.0.9000

* Initial standalone package scaffold.
* Adds R wrappers for Python `tdt.read_block()`, TDT tank-name parsing,
  stream formatting, tank discovery, completed-extraction detection, and
  per-subject extraction exports.
