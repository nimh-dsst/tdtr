% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{read_block_py}
\alias{read_block_py}
\title{Read a TDT block through Python \code{tdt}}
\usage{
read_block_py(
  block_path,
  bitwise = "",
  channel = 0,
  combine = NULL,
  headers = 0,
  nodata = FALSE,
  ranges = NULL,
  store = "",
  t1 = 0,
  t2 = 0,
  evtype = NULL,
  verbose = 0,
  sortname = "TankSort",
  export = NULL,
  scale = 1,
  dtype = NULL,
  outdir = NULL,
  prefix = NULL,
  outfile = NULL,
  dmy = FALSE,
  noepocauto = FALSE,
  ...
)
}
\arguments{
\item{block_path}{Path to a TDT block/tank directory.}

\item{bitwise, channel, combine, headers, nodata, ranges, store, t1, t2, evtype, verbose, sortname, export, scale, dtype, outdir, prefix, outfile, dmy, noepocauto}{Arguments passed to Python \code{tdt.read_block()}.}

\item{...}{Additional keyword arguments passed to Python \code{tdt.read_block()}.}
}
\value{
A \code{tdt_block_py} wrapper.
}
\description{
This is the explicit Python-backed compatibility wrapper for
\code{tdt.read_block()}. It keeps Python objects live and does not copy stream
arrays into R unless a collection helper is called later.
}
\details{
\code{read_block_py()} passes \code{store} directly to Python \code{tdt.read_block()}. For
stores whose returned names are sanitized by Python, such as \verb{_465A}, pass the
original TDT store ID (\verb{465A}) or use \code{\link[=read_block]{read_block()}} for tdtr's
header-based normalization.
}
